package com.training.ui;

import java.time.LocalDate;
import java.util.function.BiPredicate;
import java.util.function.Consumer;
import java.util.function.Predicate;

import com.training.bean.Circle;
import com.training.bean.Employee;
import com.training.bean.Gender;
import com.training.bean.Manager;
import com.training.bean.RegionalManager;

public class Main1 {
	public static void main(String[] args) {
		
		/*Consumer<Manager> consumer1;
		consumer1=(Manager m)->{
			System.out.println(m.getNetSalary());
		};
		
		Manager manager=new Manager(101, "John", 'A', LocalDate.now(), 10000.00, Gender.MALE, 10);
		
		consumer1.accept(manager);
		
		
		
		RegionalManager regionalManager=new RegionalManager(102, "Jency", 'A', LocalDate.now(), 12000, Gender.FEMALE, 90, 5);
		consumer1.accept(regionalManager);
		
		Employee employee=new Employee(103, "Karthik", 'A', LocalDate.now(), 2000, Gender.MALE);
		// consumer1.accept(employee);  error
		 
		
		 */
		
		
		
		
		
		
	}
}
